name: add
table: person
fields:
- code = demo(add(1,2))

name: test_invocation
api: public get
get:
  - demo(1)   # test macro compilation
  - x = []null_table('x'){'macro invoked'}   #test parser text macro in compilation
  - y = []dynamic_table('person'){count(code)}
  - z = as any sample_invocation
  - as any this
